import ('./function/studio.js');
class classes{
    constructor(){
        this.class = {
            "CNT_Code-Class-CNT":"CNT-Studio(import class)"
        }
        var class_file = [new studio, new getwindows];
        this.studio = class_file[0];
        this.getwindows = class_file[1];
    }
}